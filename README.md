# Dashboard

#### Prequisite

- Have Node installed

We need to install some dependencies:

```
npm install
``` 

Now let us create the production build for our application:

```
npm run build
``` 

To run the project:
```
npm run start
``` 